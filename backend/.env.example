# 后端环境配置示例
# 复制本文件为 .env 后填入实际值即可被 pydantic-settings 读取

APP_NAME=voice-calendar-backend
APP_ENV=development
APP_HOST=127.0.0.1
APP_PORT=8000

# CORS 允许的前端来源，多个用逗号分隔
CORS_ORIGINS=http://localhost:3000

# SQLite 文件路径（相对 backend/ 目录）
DATABASE_URL=sqlite:///./data/voice_calendar.db
